{
  "app": "npx ts-node --prefer-ts-exts bin/app.ts",
  "watch": {
    "include": ["**"],
    "exclude": ["README.md", "cdk*.json", "dist", "node_modules"]
  },
  "context": {
    "@aws-cdk/aws-lambda:recognizeLayerVersion": true,
    "@aws-cdk/core:stackRelativeExports": true,

    "---NOTE---": "imageTag is passed via --context imageTag=<git-sha> by the deploy workflow. CloudFront is managed separately — see infrastructure/README.md for the one-time /life-game* behavior setup.",

    "imageTag": "latest"
  }
}
