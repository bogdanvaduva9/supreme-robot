{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"systemMessage\": \"Doc check: if stacks, workflows, endpoints, or the data model changed this session — update CLAUDE.md and docs/architecture.md before closing.\"}'",
            "statusMessage": "Checking if docs need updating..."
          }
        ]
      }
    ]
  }
}
